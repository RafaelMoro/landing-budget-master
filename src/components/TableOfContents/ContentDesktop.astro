---
export interface Props {
  id: string;
  text: string;
  href: string;
}
const { text, href, id } = Astro.props;
---

<a
  id={id}
  href={href}
  class="pl-2 border-l border-solid border-gray-600 text-lg py-2 block"
>
  {text}
</a>
